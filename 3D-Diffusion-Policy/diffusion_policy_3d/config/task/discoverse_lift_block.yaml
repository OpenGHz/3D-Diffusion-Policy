name: discoverse_lift_block

task_name: lift_block

shape_meta: &shape_meta
  # acceptable types: rgb, low_dim
  obs:
    point_cloud:
      shape: [512, 3]
      type: point_cloud
    agent_pos:
      shape: [7]
      type: low_dim
  action:
    shape: [7]

# env_runner:
#   _target_: diffusion_policy_3d.env_runner.discoverse_runner.DiscoverseRunner
#   eval_episodes: 20
#   max_steps: 300
#   n_obs_steps: ${n_obs_steps}
#   n_action_steps: ${n_action_steps}
#   fps: 10
#   task_name: lift_block
#   render_size: 84
#   use_point_crop: ${policy.use_point_crop}

env_runner: null

dataset:
  _target_: diffusion_policy_3d.dataset.discoverse_dataset.DiscoverseDataset
  zarr_path: data/zarr/lift_block.zarr
  horizon: ${horizon}
  pad_before: ${eval:'${n_obs_steps}-1'}
  pad_after: ${eval:'${n_action_steps}-1'}
  seed: 42
  val_ratio: 0.02
  max_train_episodes: 90
